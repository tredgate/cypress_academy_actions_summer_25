name: Cypress - Every 15 minutes (CRON)

on:
  schedule:
    - cron: "*/15 19-21 * * *"

jobs:
  cypress-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Cypress run
        uses: cypress-io/github-action@v6
